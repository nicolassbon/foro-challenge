# Este archivo contiene todas las variables de entorno necesarias para ejecutar la aplicación.
# Copia este archivo como ".env" y ajusta los valores según tu entorno.

# Nombre de la base de datos
DB_NAME=forohub

# Usuario de MySQL
DB_USERNAME=forohub_user

# Contraseña del usuario de MySQL
DB_PASSWORD=forohub_pass

# Base de datos para Docker
MYSQL_DATABASE=forohub

# Usuario de MySQL para Docker
MYSQL_USER=forohub_user

# Contraseña del usuario MySQL para Docker
MYSQL_PASSWORD=forohub_pass

# Contraseña del usuario root de MySQL para Docker
MYSQL_ROOT_PASSWORD=root

# Secret key para firmar los tokens JWT
# DEBE ser un string de al menos 256 bits (32 caracteres)
# Genera uno nuevo con: openssl rand -base64 64
API_SECURITY_TOKEN_SECRET=tu_secret_key_de_al_menos_256_bits_aqui

# Tiempo de expiración del token en milisegundos
API_SECURITY_TOKEN_EXPIRATION=3600000

# ====================================
# NOTAS
# ====================================
#
# 1. Los valores con DB_* se usan en application.yaml
#
# 2. Los valores con MYSQL_* se usan en docker-compose.yml
#
# 3. Si no usas Docker, solo necesitas las variables DB_* y API_*
